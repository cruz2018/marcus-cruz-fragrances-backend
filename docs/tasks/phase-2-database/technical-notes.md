# Phase 2 — Database (Technical Notes)

- Define schema in prisma/schema.prisma.
- Run: npx prisma migrate dev --name init.
- Generate client: prisma generate (runs automatically on migrate dev).
- Seed: prisma db seed.
- Validate by running the API and testing a query.
